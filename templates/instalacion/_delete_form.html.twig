<form method="post" action="{{ path('app_instalacion_delete', {'id': instalacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ instalacion.id) }}">
    <button class="btn">Delete</button>
</form>
